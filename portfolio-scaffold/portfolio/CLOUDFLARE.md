# Cloudflare Pages build settings (reference — configure these in the CF dashboard)
# Build command:   npm run build
# Output dir:      dist
# Node version:    20

# No wrangler.toml needed for a purely static Astro site on CF Pages.
# Just connect your GitHub repo in the CF Pages dashboard and set:
#   Framework preset: Astro
#   Build command:    npm run build
#   Build output dir: dist
